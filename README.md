# Task-3-Simple-Auth-Mock

🚀 How to Run

Save server.js → run npm init -y && npm install express body-parser cors.

Start backend → node server.js.

Open index.html in your browser.

Try login with:

✅ username: admin, password: 12345 → success.

❌ anything else → error.
